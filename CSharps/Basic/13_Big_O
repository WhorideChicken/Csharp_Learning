[Big-O 표기법] 
: 알고리즘을 분석할 때 사용하는 기본적인 지표
* 알고리즘의 효율을 측정하는 방법

1단계 : 수행되는 연산의 개수를 대략적으로 판단.
 - for문 1개당 N
 - 이중 for문은 N^2
 - 덧셈등 사칙연산 +1

 2단계 : 대장만 남긴다.
 - 영향력이 가장큰 항목만 남기고 삭제
 - 상수는 무시한다 2N -> N
 
 ex)1 + N + 4*N^2+1
 => O(N^2)